to run python servere: flask run
to check the return of API : http://127.0.0.1:5000/drinks/2
    here : 
            1. drinks are the dictonary of drinks
            2. you can change the drinks/<id> with 0..n to iterate between the dictonary values
            
make sure you install all the requirement.txt using `pip install requirement.txt`
